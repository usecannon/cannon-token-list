import { ChainBuilderRuntime, ChainDefinition, ContractArtifact, DeploymentInfo, IPFSLoader, build, createInitialContext, publishPackage } from "@usecannon/builder";

export async function publishPackages() {
  publishPackage()
}

publishPackages()